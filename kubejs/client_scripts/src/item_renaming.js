ClientEvents.lang('en_us', event => {
  event.renameItem('croptopia:salt_ore', 'Salt Block') // Only changes the name of the ITEM, not the BLOCK
  event.renameBlock('croptopia:salt_ore', 'Salt Block') // Only changes the name of the BLOCK, not the ITEM

})